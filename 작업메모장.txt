모든 요청에 대한 비어있는 요청은 저장하지 않는다 -- clear
POST 테스트가 너무 긴가? -- clear
테이블에 아이템 여러개 표시하기 -- clear
하나이상의 목록 지원하기 -- clear

모델을 조정해서 아이템이 다른 목록과 연계되도록 한다  -- clear
각 목록별 고유 URL을 추가한다  -- clear
POST를 이용해서 새로운 목록을 생성하는 URL을 추가한다  -- clear 
POST를 이용해서 새로운 아이템을 기존 목록에 추가하는 URL을 만든다.  -- clear
urls.py 에 있는 중복 코드를 리팩터링 한다 -- clear

views.py에서 하드코딩된 url제거 -- clear
list.html과 home.html의 하드코딩된 url제거 -- clear
view에 있는 중복된 검증 로직 제거
